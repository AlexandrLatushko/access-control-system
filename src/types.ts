export type User = {
    id: number
    name: string
    email: string
    role: string
    accessLevel: number
  }
  
  export type Log = {
    id: number
    message: string
    timestamp: string
  }
  